[pytest]
addopts = --headed --slowmo=100
python_classes = Test Flow
markers =
    slow: marks tests as slow due to multiple consecutive API calls (deselect with -m "not slow")
    flow: marks full end-to-end flow tests intended for prod/UAT (deselect with -m "not flow")
